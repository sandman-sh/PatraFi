{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd patrafi && npm install && npm run build",
  "outputDirectory": "patrafi/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
